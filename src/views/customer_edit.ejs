<%- include ('partials/_header'); %>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-5">
            <div class="card">
                <div class="card-body">
                    <form action="/update/<%= data.id %>" methos="post">
                        <div class="form-group">
                           <input type="text" name="name" value ="<%= data.name %>" class="form-control" autofocus>
                          </div>
                          <div class="form-group">
                            <input type="text" name="numPlays" value ="<%= data.numPlays %>" class="form-control">
                          </div>
                          <div class="form-group">
                            <input type="text" name="numWins" value ="<%= data.numWins %>" class="form-control">
                          </div>
                          <div class="form-group">
                              <input type="text" name="numLost" value ="<%= data.numLost %>" class="form-control">
                            </div>
                        <button type="submit" class="btn btn-primary"> edit customer </button>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include ('partials/_footer'); %>
